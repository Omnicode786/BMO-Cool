# Runtime model files

`./scripts/fetch_models.sh` downloads the official MediaPipe Hand Landmarker float16 task model to:

`models/hand_landmarker.task`

The binary is intentionally fetched at install time instead of being redistributed in this repository. The URL is the official Google-hosted MediaPipe model asset configured by the project.
